# zerotier-setup-openwrt
A simple script to automate the installation and configuration of ZeroTier on OpenWRT. It installs the necessary packages, configures the ZeroTier interface, sets up firewall rules, and restarts services. This script is perfect for users who want to quickly set up a ZeroTier network on their OpenWRT devices without manual intervention.
